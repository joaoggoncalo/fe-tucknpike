import 'package:flutter/material.dart';

/// Brand colors used throughout the application.
class BrandColors {
  /// Dark blue color.
  static const primaryColor = Color(0xFF03045E);

  /// Light blue color.
  static const accentColor = Color(0xFF0077B6);

  /// Light beige color.
  static const backgroundColor = Color(0xFFFBF8EF);

  /// Light gray color.
  static const cardColor = Color(0xFFE5E2D9);

  /// Light cyan color.
  static const lightAccent = Color(0xFFCAF0F8);

  /// Dark blue color.
  static const darkAccent = Color(0xFF002338);
}
