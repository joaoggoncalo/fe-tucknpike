import 'package:flutter/material.dart';

/// The trainings page.
class TrainingsPage extends StatelessWidget {
  /// Creates the trainings page.
  const TrainingsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
